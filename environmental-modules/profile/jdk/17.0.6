#%Module

proc ModulesHelp { } {
       global version modroot
       puts stderr "\n\tThis adds $modroot/* to several of the"
       puts stderr "\tenvironment variables."
       puts stderr "\n\tVersion $version\n"
}

module-whatis    "loads the jdk 17.0.6 environment."

conflict jdk

# for Tcl script use only
set    version        17.0.6
set    modroot        /Library/Java/JavaVirtualMachines/jdk-17.jdk/Contents/Home

setenv        JAVA_HOME    $modroot
prepend-path    PATH           $modroot/bin
prepend-path    MANPATH        $modroot/man